<Window x:Class="kursach.Booking1"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:kursach"
        xmlns:mvvm="clr-namespace:kursach.View" 
        DataContext="{DynamicResource vm}"
        mc:Ignorable="d"
        Title="Booking1" Height="450" Width="800" Background="#88d3c6">
    <Window.Resources>
        <mvvm:BookingMvvm x:Key="vm"/>
    </Window.Resources>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*" />
            <ColumnDefinition Width="2*" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="3*"/>
            <RowDefinition />


        </Grid.RowDefinitions>

        <StackPanel Grid.Column="0" Margin="10" DataContext="{Binding NewBooking}" >
            <Label Content="Дата заеда"/>
            <TextBox Text="{Binding Datestart}" Height="30"/>
            <Label Content="Дата выезда"/>
            <TextBox Text="{Binding Dateend}"  Height="30"/>
            <Label Content="Статус"/>
            <TextBox Text="{Binding Status}"  Height="30"/>
        </StackPanel>


        <StackPanel Grid.Column="0" Grid.Row="2" Orientation="Horizontal" VerticalAlignment="Bottom" Margin="10">
            <Button Content="Бронировать" Width="150" Height="40" Background="LightCyan" Command="{Binding InsertBooking}" />
        </StackPanel>

        <StackPanel Grid.Column="1" Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="10">
            <Button Content="На главную" Width="100" Height="40" Background="LightCyan" Click="NavigateButton_Click"/>
        </StackPanel>
        <StackPanel Orientation="Horizontal" Margin="0,0,0,10"  HorizontalAlignment="Center" VerticalAlignment="Bottom">
            <TextBlock Text="Свободные номера:" Margin="0,0,10,0" FontSize="18"/>
            <ComboBox x:Name="AvailableRoomsComboBox" Width="200" Height="50" ItemsSource="{Binding AvailableRooms}"  SelectedItem="{Binding SelectedRoom}" DisplayMemberPath="Numberroom"/>
        </StackPanel>

        <!-- Таблица гостей -->
        <DataGrid x:Name="GuestDataGrid"   Grid.Column="1"  ItemsSource="{Binding Guests}" AutoGenerateColumns="False" IsReadOnly="True">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Имя" Binding="{Binding FirstName}"/>
                <DataGridTextColumn Header="Фамилия" Binding="{Binding Surname}"/>
                <DataGridTextColumn Header="Отчество" Binding="{Binding Lastname}"/>
                <DataGridTextColumn Header="Телефон" Binding="{Binding Phone}"/>
                <DataGridTextColumn Header="Email" Binding="{Binding Email}"/>
                <DataGridTextColumn Header="Паспорт" Binding="{Binding Passportdata}"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Window>
