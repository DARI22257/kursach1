<Window x:Class="kursach.Booking1"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:kursach"
        mc:Ignorable="d"
        Title="Booking1" Height="450" Width="800" Background="#88d3c6">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*" />
            <ColumnDefinition Width="2*" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Column="0" Margin="10" DataContext="{Binding NewBooking1}" >
            <Label Content="Дата заеда"/>
            <TextBox Text="{Binding Datestart}"/>
            <Label Content="Дата выезда"/>
            <TextBox Text="{Binding Dateend}"/>
            <Label Content="Статус"/>
            <TextBox Text="{Binding Status}"/>
        </StackPanel>

        <ListView Grid.Column="1" ItemsSource="{Binding Booking1}" SelectedItem="{Binding SelectBooking1}" >
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Дата заезда в номер" DisplayMemberBinding="{Binding Lastname}" Width="100"/>
                    <GridViewColumn Header="Дата вызда" DisplayMemberBinding="{Binding Surname}" Width="100"/>
                    <GridViewColumn Header="Статус" DisplayMemberBinding="{Binding Phone}"/>
                </GridView>
            </ListView.View>

        </ListView>

        <StackPanel Grid.Column="0" Grid.Row="1" Orientation="Horizontal" VerticalAlignment="Bottom" Margin="10">
            <Button Content="Бронировать" Width="150" Height="40" Background="LightCyan" Command="{Binding InsertBooking1}" />
        </StackPanel>

        <StackPanel Grid.Column="1" Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right" Margin="10">
            <Button Content="На главную" Width="100" Height="40" Background="LightCyan"/>
        </StackPanel>


    </Grid>
</Window>
