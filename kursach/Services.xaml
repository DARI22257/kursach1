<Window x:Class="kursach.Services"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:kursach"
        xmlns:mvvm="clr-namespace:kursach.View" 
        DataContext="{DynamicResource vm}"
        mc:Ignorable="d"
        Title="Services" Height="300" Width="600" >
    <Window.Resources>
        <mvvm:ServicesMvvm x:Key="vm"/>

        <Style TargetType="Button">
            <Setter Property="Background" Value="#90CAF9"/>
            <Setter Property="Foreground" Value="Black"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Padding" Value="10,5"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="BorderBrush" Value="#42A5F5"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
        BorderBrush="{TemplateBinding BorderBrush}"
        BorderThickness="{TemplateBinding BorderThickness}"
        CornerRadius="8"
        Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>
    <Grid Background="#A0E0D5" Margin="20">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*" />
            <ColumnDefinition Width="2*" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Column="0" Margin="10" DataContext="{Binding NewServices}" >
            <Label Content="Цена"/>
            <TextBox Text="{Binding Price}"/>
            <Label Content="Название"/>
            <TextBox Text="{Binding Title}"/>


        </StackPanel>

        <StackPanel Orientation="Horizontal" Margin="0,15,0,0" Grid.Row="1" Grid.ColumnSpan="2">
            <Button Content="Зарегистрировать " Command="{Binding InsertServices}" />
            <Button Content="Удалить " Command="{Binding RemovesServices}" Margin="10,0,0,0"/>
            <Button Content="Редактировать " Command="{Binding UpdateServices}" Margin="10,0,0,0"/>
        </StackPanel>

        <ListView Grid.Column="1" ItemsSource="{Binding Services}" SelectedItem="{Binding SelectedService}">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Цена" DisplayMemberBinding="{Binding Price}" Width="100"/>
                    <GridViewColumn Header="Название" DisplayMemberBinding="{Binding Title}" Width="150"/>
                </GridView>
            </ListView.View>

        </ListView>

        <StackPanel Grid.Column="1" Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right" Margin="10">
            <Button Content="На главную" Width="100"  Background="LightCyan" Click="NavigateButton_Click"/>
            
        </StackPanel>

    </Grid>
</Window>
