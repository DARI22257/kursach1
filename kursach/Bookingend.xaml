<Window x:Class="kursach.Bookingend"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:kursach"
        mc:Ignorable="d"
        Title="Bookingend" Height="450" Width="800">


    <Window.Resources>
        <Style TargetType="Button">
            <Setter Property="Background" Value="#90CAF9"/>
            <Setter Property="Foreground" Value="Black"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Padding" Value="10,5"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="BorderBrush" Value="#42A5F5"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                    BorderBrush="{TemplateBinding BorderBrush}"
                    BorderThickness="{TemplateBinding BorderThickness}"
                    CornerRadius="8"
                    Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid Background="#A0E0D5" Margin="20">
        <StackPanel Margin="20">
            <TextBlock Text="Информация о бронировании" FontWeight="Bold" FontSize="20" HorizontalAlignment="Center" Margin="0,0,0,10"/>

            <TextBlock Text="Гость:" FontWeight="Bold"/>
            <TextBlock x:Name="GuestNameText"/>

            <TextBlock Text="Телефон:" FontWeight="Bold"/>
            <TextBlock x:Name="PhoneText"/>

            <TextBlock Text="Номер комнаты:" FontWeight="Bold"/>
            <TextBlock x:Name="RoomNumberText"/>

            <TextBlock Text="Тип комнаты:" FontWeight="Bold"/>
            <TextBlock x:Name="RoomTypeText"/>

            <TextBlock Text="Даты бронирования:" FontWeight="Bold"/>
            <TextBlock x:Name="BookingDatesText"/>

            <TextBlock Text="Статус:" FontWeight="Bold"/>
            <TextBlock x:Name="StatusText"/>

            <Button Content="Подтвердить бронь" HorizontalAlignment="Center" VerticalAlignment="Bottom" Margin="10"  Width="200" Height="50" Click="ConfirmBooking_Click"/>
        </StackPanel>
    </Grid>
</Window>
